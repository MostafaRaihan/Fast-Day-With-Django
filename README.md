# 🚀 Django Back-End ডেভেলপমেন্ট


## ১. 🛠️ প্রাথমিক সেটআপ ও টুলস

| বিষয়বস্তু | বিবরণ | কমান্ড/ফাইল |
| :--- | :--- | :--- |
| **Django কি?** | একটি হাই-লেভেল পাইথন ওয়েব ফ্রেমওয়ার্ক (The web framework for perfectionists with deadlines)। | N/A |
| **Stable ভার্সন** | প্রোডাকশনের জন্য সবসময় সর্বশেষ স্টেবল ভার্সন ব্যবহার করুন। | N/A |
| **Virtual Environment** | প্রোজেক্টের ডিপেন্ডেন্সিগুলো আলাদা রাখতে ব্যবহৃত হয়। | `python -m venv .venv` |
| **প্যাকেজ ডেফিনেশন** | ইন্সটল করা প্যাকেজগুলোর তালিকা তৈরি করা। | `pip freeze > requirements.txt` |
| **প্যাকেজ ইন্সটলেশন** | নতুন বা রিসেট করা পরিবেশে প্যাকেজ পুনরায় ইন্সটল করা। | `pip install -r requirements.txt` |
| **Venv সক্রিয়করণ** | ভার্চুয়াল পরিবেশ চালু করা। | Win: `.venv\Scripts\activate` <br> Linux/macOS: `source .venv/bin/activate` |

---

## ২. 📂 Django প্রোজেক্টের কাঠামো (First Look)

| ফাইল/ফোল্ডার | কাজ |
| :--- | :--- |
| **`manage.py`** | প্রোজেক্ট প্রশাসনের জন্য কমান্ড-লাইন ইউটিলিটি। |
| **`<ProjectName>/settings.py`** | মূল কনফিগারেশন, যেমন: `INSTALLED_APPS`, ডাটাবেস, টেমপ্লেট পাথ। |
| **`<ProjectName>/urls.py`** | প্রোজেক্টের সকল URL প্যাটার্ন ডিস্ট্রিবিউট করা। (Default: `/admin/`) |
| **`<ProjectName>/wsgi.py, asgi.py`** | ওয়েব সার্ভার গেটওয়ে ইন্টারফেস (Synchronous & Asynchronous) কনফিগারেশন। |
| **`templates/`** | HTML টেমপ্লেট ফাইল রাখার ডিরেক্টরি। |

---

## ৩. ✨ প্রোজেক্ট ও অ্যাপ তৈরি এবং সংযোগ

| ধাপ | কমান্ড | কনফিগারেশন |
| :--- | :--- | :--- |
| **১. প্রোজেক্ট তৈরি** | `django-admin startproject DjangoProject1` | N/A |
| **২. সার্ভার চালু** | `python manage.py runserver` | Initial Interface |
| **৩. ডাটাবেস মাইগ্রেশন** | `python manage.py migrate` | প্রাথমিক ডাটাবেস টেবিল তৈরি। |
| **৪. অ্যাপ তৈরি** | `python manage.py startapp myapp` | অ্যাপের ডিফল্ট কাঠামো তৈরি। |
| **৫. অ্যাপ সংযোগ** | `settings.py` এ `INSTALLED_APPS` এর মধ্যে `'myapp'` যোগ করা। | `INSTALLED_APPS = [..., 'myapp']` |
| **৬. URL কনফিগারেশন** | প্রোজেক্টের `urls.py` এ অ্যাপের URL অন্তর্ভুক্ত করা। | `path('', include('myapp.urls'))` |

---

## ৪. 🗂️ অ্যাপের অভ্যন্তরীণ কাঠামো (`myapp` এর ভিতরে)

| ফাইল/ফোল্ডার | মূল ভূমিকা |
| :--- | :--- |
| **`models.py`** | ডাটাবেস স্কিমা (ORM - Object-Relational Mapping) ডেফিনেশন। |
| **`views.py`** | ইউজার রিকোয়েস্ট গ্রহণ করা এবং রেসপন্স (যেমন: HTML/JSON) তৈরি করা। |
| **`urls.py`** | অ্যাপ-নির্দিষ্ট URL প্যাটার্ন ও View ফাংশনের ম্যাপিং। |
| **`admin.py`** | অ্যাডমিন প্যানেলে মডেলগুলো রেজিস্ট্রেশন করার জন্য। |
| **`migrations/`** | মডেল পরিবর্তনগুলি ট্র্যাক করার ফাইল। |

### 💡 প্রথম Request-Response তৈরি:
1. `myapp/views.py` এ একটি ফাংশন তৈরি করুন।
2. `myapp/urls.py` তৈরি করুন এবং ফাংশনটিকে একটি URL এর সাথে যুক্ত করুন।
3. `views.py` এ `HttpResponse` বা `render()` ব্যবহার করে আউটপুট দিন।

---

## ৫. 🖥️ Django টেমপ্লেট মাস্টারি

**তারিখ: ২৫ নভেম্বর**

Django টেমপ্লেট ল্যাঙ্গুয়েজ (DTL) ব্যবহার করে ডাইনামিক HTML রেন্ডার করার পদ্ধতি:

| বিষয়বস্তু | বিবরণ | উদাহরণ |
| :--- | :--- | :--- |
| **View থেকে রেন্ডার** | `render()` ফাংশন ব্যবহার করে কনটেক্সট সহ টেমপ্লেট পাঠানো। | `return render(request, 'index.html', context)` |
| **Template Tags** | কন্ট্রোল ফ্লো (লুপ, কন্ডিশন) বাস্তবায়ন। | `{% for item in list %}` `{% if condition %}` |
| **Template Filters** | ভেরিয়েবলগুলোর আউটপুট ফরম্যাট পরিবর্তন করা। | `{{ value\|date:"Y-m-d" }}` |
| **Architecture (Layout)** | **Inheritance** ব্যবহার করে কোড পুনরাবৃত্তি কমানো। | `{% extends "base.html" %}` এবং `{% block content %}` |

---

## ৬. 💻 অন্যান্য গুরুত্বপূর্ণ বিষয়

* **PyCharm Config**: প্রোজেক্ট interpreter হিসেবে `.venv` সেটআপ করা এবং রান/ডিবাগ কনফিগারেশন তৈরি করা।
* **Debug Process**: সঠিক Error Tracing এবং Breakpoint ব্যবহার করে কোড ডিবাগ করা।
